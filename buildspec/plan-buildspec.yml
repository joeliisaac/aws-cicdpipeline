version: 0.2
phases:
  pre_build:
    commands:
      - terraform init
      - terraform validate
      - pip install boto3
      -  pip install checkov
  
  build:
    commands:
      - terraform plan